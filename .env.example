# Bước 1: copy file mẫu thành file thật
cp .env.example .env

# Bước 2: mở file .env (KHÔNG phải .env.example) và điền:
DISCORD_TOKEN=<token thật của bạn lấy từ Discord Developer Portal>
DEV_GUILD_ID=<id server test, có thể để trống>
